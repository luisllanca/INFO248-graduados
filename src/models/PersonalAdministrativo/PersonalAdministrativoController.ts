import PersonalAdministrativoModel from "./PersonalAdministrativoModel";
import { Identifier } from "sequelize";

class PersonalAdministrativoController {
    async CambiarPass(id: Identifier, password: string): Promise<void>{
       //cambiar la base de dato
    }
}