//LLamar EstudianteModel
//Llamar Estudiante
// instanciar los datos y traspasarlos.
// import { Estudiante } from "../../controllers/Usuario/Estudiante";
// import EstudianteModel from "../Estudiante/EstudianteModel";

// export default class EstudiantePersistance {
//   async estudianteModel(id: number) {
//     const estudiantes = await EstudianteModel.findByPk(id, {
//       attributes: { exclude: ["createdAt", "updatedAt"] },
//     });
//     const comprobantes = await ComprobanteModel.findByPk(id, {
//       attributes: { exclude: ["createdAt", "updatedAt"] },
//     });
//     const becas = await becaModel.findByPk(id, {
//       attributes: { exclude: ["createdAt", "updatedAt"] },
//     });
//     const estudiante = new Estudiante();
//     return estudiante;
//   }
// }
